{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": ["**/.env.*local"],
    "globalEnv": ["VITE_ENV", "VITE_PORT", "VITE_URL", "VITE_API_URL"],
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "dev": {
            "cache": false,
            "dependsOn": ["^build"],
            "persistent": true
        },
        "lint": {},
        "test": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "type-check": {
            "cache": true,
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        }
    }
}
